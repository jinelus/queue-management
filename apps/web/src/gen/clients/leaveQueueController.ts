/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  LeaveQueueController401,
  LeaveQueueController404,
  LeaveQueueControllerMutationResponse,
  LeaveQueueControllerPathParams,
} from '../types/LeaveQueueController.ts'
import type { RequestConfig, ResponseErrorConfig } from './src/lib/api-client'
import fetch from './src/lib/api-client'

function getLeaveQueueControllerUrl(
  organizationId: LeaveQueueControllerPathParams['organizationId'],
  ticketId: LeaveQueueControllerPathParams['ticketId'],
) {
  const res = {
    method: 'PATCH',
    url: `/organizations/${organizationId}/tickets/${ticketId}/leave-queue` as const,
  }
  return res
}

/**
 * @description Leave the queue for a ticket within an organization.
 * @summary Leave the queue for a ticket
 * {@link /organizations/:organizationId/tickets/:ticketId/leave-queue}
 */
export async function leaveQueueController(
  organizationId: LeaveQueueControllerPathParams['organizationId'],
  ticketId: LeaveQueueControllerPathParams['ticketId'],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    LeaveQueueControllerMutationResponse,
    ResponseErrorConfig<LeaveQueueController401 | LeaveQueueController404>,
    unknown
  >({
    method: 'PATCH',
    url: getLeaveQueueControllerUrl(organizationId, ticketId).url.toString(),
    ...requestConfig,
  })
  return res
}
