/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  TransferTicketController401,
  TransferTicketController404,
  TransferTicketControllerMutationRequest,
  TransferTicketControllerMutationResponse,
  TransferTicketControllerPathParams,
} from '../types/TransferTicketController.ts'
import type { RequestConfig, ResponseErrorConfig } from './src/lib/api-client'
import fetch from './src/lib/api-client'

function getTransferTicketControllerUrl(
  organizationId: TransferTicketControllerPathParams['organizationId'],
  ticketId: TransferTicketControllerPathParams['ticketId'],
) {
  const res = {
    method: 'PUT',
    url: `/organizations/${organizationId}/tickets/${ticketId}/transfer` as const,
  }
  return res
}

/**
 * @description Transfer a ticket to another service within an organization. Optionally specify a target staff member.
 * @summary Transfer a ticket to another service or colleague
 * {@link /organizations/:organizationId/tickets/:ticketId/transfer}
 */
export async function transferTicketController(
  organizationId: TransferTicketControllerPathParams['organizationId'],
  ticketId: TransferTicketControllerPathParams['ticketId'],
  data: TransferTicketControllerMutationRequest,
  config: Partial<RequestConfig<TransferTicketControllerMutationRequest>> & {
    client?: typeof fetch
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const requestData = data

  const res = await request<
    TransferTicketControllerMutationResponse,
    ResponseErrorConfig<TransferTicketController401 | TransferTicketController404>,
    TransferTicketControllerMutationRequest
  >({
    method: 'PUT',
    url: getTransferTicketControllerUrl(organizationId, ticketId).url.toString(),
    data: requestData,
    ...requestConfig,
  })
  return res
}
